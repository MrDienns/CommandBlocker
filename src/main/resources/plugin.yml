main: com.dyescape.commanddeleter.CommandDeleter
name: CommandDeleter
version: ${project.version}
website: ${project.url}
description: ${project.description}
author: MrDienns
commands:
  commanddeleter:
    description: Base plugin command